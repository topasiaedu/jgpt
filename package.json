{
  "name": "jgpt",
  "version": "0.1.0",
  "private": true,
  "description": "Jeff Teaching Graph monorepo. Webapp lives in apps/web.",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "dev:turbo": "npm run dev:turbo --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web",
    "test:probe": "npm run test:probe --prefix apps/web"
  }
}
