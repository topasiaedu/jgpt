# Jeff Teaching Graph wiki

Student-facing teaching knowledge (after sufficiency). Compiled from immutable sources in `raw/jeff/`.

## Folders

| Path | Contents |
| --- | --- |
| `frameworks/` | Teaching frameworks (Suggested until confirmed by ingest) |
| `principles/` | Durable principles |
| `claims/` | Bound, citable claims |
| `terms/` | Definitions |
| `stories/` | Cases / anecdotes from Jeff sources |
| `sources/` | Compiled source summaries after ingest |
| `voice/` | Distilled voice exemplars from sources (agent bank also in `schema/voice/`) |
| `draft/` | File-back candidates awaiting human approval |

## Rules

- Do not invent Jeff IP. Label scaffolds **Suggested / draft**.
- Every claim page should point at a source under `sources/` (and ultimately `raw/jeff/`).
- Agents: follow `../schema/AGENTS.md`. Query mounts this tree + `jeff-graph/` only for teaching answers.
