# Voice exemplars (compiled)

Distill short exemplars from Jeff sources into this folder after ingest.

**App source of truth:** `schema/voice/sound-profile.md` (plus `jeff-style.md`, `do-dont.md`).  
Do not fork conflicting rules here. This folder is for source-grounded quotes and paraphrase exemplars only; point agents and the webapp at `schema/voice/`.

After prebuild sync, the webapp also copies `schema/voice/*.md` → `apps/web/content/jeff/voice/`.
